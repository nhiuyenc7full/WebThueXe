@model IEnumerable<HKT2tr5.Models.Entities.NhaSanXuat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenNSX)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenNSX)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NhaSanXuatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NhaSanXuatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NhaSanXuatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
